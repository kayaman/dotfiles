# Machine-specific (not tracked)
bash/.bash_local

# OS
.DS_Store
Thumbs.db

# User secrets / machine-specific config
dotfiles.toml
dotfiles.sops.toml
.env
.env.sh
.claude

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform.lock.hcl

# AppImage extractions / binaries (never belong in dotfiles)
squashfs-root/
*.AppImage
